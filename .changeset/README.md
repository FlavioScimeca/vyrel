# Changesets

This folder holds changeset files created with `bun run changeset`.

When changes are ready to release, run `bun run version-packages` to bump versions and update changelogs, then `bun run release` to build and publish to npm.
